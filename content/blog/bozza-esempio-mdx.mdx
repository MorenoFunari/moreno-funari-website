---
title: "Bozza di esempio MDX"
description: "Contenuto locale usato per verificare il motore editoriale del blog."
publishedAt: "2026-06-28"
updatedAt: "2026-06-28"
category: "Sviluppo"
tags:
  - mdx
  - verifica
draft: true
featured: false
coverImage:
coverAlt:
---

Questo file serve solo a verificare il motore editoriale locale. Non e un contenuto pensato per la pubblicazione.

## Sezione di verifica

Un paragrafo con testo **in grassetto**, testo *in corsivo* e un frammento di `codice inline`.

### Elementi base

- Primo elemento puntato.
- Secondo elemento puntato.
- Terzo elemento puntato.

1. Primo passaggio numerato.
2. Secondo passaggio numerato.
3. Terzo passaggio numerato.

> Questa citazione verifica la resa del blockquote.

Un link interno verso [la pagina contatti](/contatti) e un link esterno verso [Next.js](https://nextjs.org).

```ts
const stato = "bozza";
console.log(`Contenuto in ${stato}`);
```

<Callout title="Da ricordare">
  Questo callout verifica un componente editoriale disponibile senza import nel file MDX.
</Callout>

<ReflectionPrompt title="Una domanda per te">
  Questo spazio verifica il componente ReflectionPrompt senza campi input o salvataggio.
</ReflectionPrompt>
